# Node / Vite / SvelteKit
node_modules/
.svelte-kit/
build/
dist/
.DS_Store
*.log

# Tauri / Rust
src-tauri/target/
src-tauri/gen/

# Chave privada do updater — NUNCA versionar (vai como secret no GitHub)
src-tauri/updater.key
src-tauri/updater.key.pub

# Local
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example

# Conversas exportadas do ZapResponder — dados reais de clientes (LGPD).
# NUNCA versionar no repo público. Importadas para o Supabase via scripts/.
conversas/*.json
conversas/._*

# Screenshots de teste — podem conter conversas reais de clientes (LGPD).
.claude/skills/run-interno-uplab/screenshots/

# Arquivo de referência colado de outro projeto (contém chaves/schema).
ENV DO OUTRO PROJETO
